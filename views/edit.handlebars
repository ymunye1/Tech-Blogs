<div class="row">
  <div class="col-auto">
    {{!-- <h2>Welcome, {{username}}!</h2> --}}
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Edit Entry:</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="project-name">title:</label>
        <textarea class="form-input"  id="project-name" name="project-name" >{{name}}</textarea>
      </div>
      {{!-- <div class="form-group">
        <label for="project-funding">needed funding ($):</label>
        <input class="form-input" type="number" id="project-funding" name="project-funding" />
      </div> --}}
      <div class="form-group">
        <label for="project-desc">content:</label>
        <textarea class="form-input" id="project-desc" name="project-desc">{{description}}</textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-sm btn-danger edit" data-id="{{id}}">Update</button>
      </div>
    </form>
  </div>


<script src="/js/edit.js"></script>
